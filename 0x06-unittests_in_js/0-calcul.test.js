/**
 * test function calculateNumber
 */

const assert = require('assert');
const calculateNumber = require("./0-calcul.js");

describe('Test calculateNumber', () => {
  it('test 1, 3', () => {
    assert.strictEqual(1 + 3, 4);
  });

});
